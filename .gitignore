# Go build outputs
bin/
*.exe
*.test
*.out

# Go module vendor directory (rely on go.sum + module cache instead)
vendor/

# Environment — copy .env.example → .env and fill in your values
.env

# Tenant config — copy config/tenants.example.json → config/tenants.json and fill in your tenants
config/tenants.json

# Evaluation reports (generated, not committed)
tmp/

# macOS
.DS_Store

# JetBrains / VSCode
.idea/
.vscode/
*.swp
docs/smythos-sas-agents/
